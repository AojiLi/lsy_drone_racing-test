# v62d_005 Decision

Date: 2026-06-27

## Decision

Outcome:

```text
switch_candidate_family
```

Do not promote `v62d_005_speedbin_value_scale10_30m`.

Current frontier remains:

```text
v62c_7m
lsy_drone_racing/control/checkpoints/v62c_tanh_squashed_gaussian_10m/v62c_tanh_squashed_gaussian_10m_step_007340032.pkl
```

## Evidence

Analysis packet:

```text
experiments/level3_ppo_loop/analysis/2026-06-27_v62d_005_speedbin_value_scale10_30m_analysis.md
```

Subagent reviews:

```text
experiments/level3_ppo_loop/analysis/2026-06-27_v62d_005_speedbin_value_scale10_30m_subagent_reviews.md
```

Best v62d_005 checkpoint:

```text
15M
lsy_drone_racing/control/checkpoints/v62d_005_speedbin_value_scale10_30m/v62d_005_speedbin_value_scale10_30m_step_015000000.pkl
```

It is not promoted because:

```text
velocity is 34.2% worse than v62c 7M default
velocity is 21.4% worse than the v62c 7M speed-bin baseline
velocity is 28.3% worse than v62d_004 5M
action_delta is about 99.8x worse than v62d_004 5M
best-checkpoint audit reports action_sampling_logprob=bad
final checkpoint is worse than the best milestone
```

The candidate did improve critic/value diagnostics:

```text
explained_variance ~= 0.731
value_loss ~= 0.060
advantage_scale=ok on the trained checkpoint audit
```

But this did not convert into tracker quality. The policy became too aggressive
and velocity obedience regressed.

## Next Candidate

Launch support for:

```text
v62d_006_speedbin_longrollout_256x128_30m
```

Family:

```text
D_PPO_stabilizer
```

Changed knobs:

```text
command_generator_profile=speed_bin_balanced
num_envs=256
num_steps=128
value_target_scale=1.0
command_vel_error_coef=default
num_minibatches=4
update_epochs=1
```

This intentionally keeps the useful v62d_004 speed-bin generator but changes the
rollout horizon from `32` to `128` simulator steps. The hypothesis is that
brake, low-speed-through, and recover behavior need longer temporal credit than
`1024 envs x 32 steps` provides.

Builder/checker support is required before any 30M training because rollout
geometry changes training semantics. Do not launch the 30M candidate until the
support smoke/checker verifies:

```text
config/level3.toml unchanged
config/level3_tracker_free_space.toml unchanged
observation_layout=level3_reference_tracker_command_v3
action_distribution=tanh_squashed_gaussian
reward_coefficients={}
command_generator_profile=speed_bin_balanced
num_envs=256
num_steps=128
checkpoint metadata records the rollout geometry
action/logprob audit is clean enough for support
```

