# v62d_004 Decision

Date: 2026-06-27

## Decision

Outcome:

```text
switch_candidate_family
```

Do not promote `v62d_004_speed_bin_balanced_generator_30m`.

Current frontier remains:

```text
v62c_7m
lsy_drone_racing/control/checkpoints/v62c_tanh_squashed_gaussian_10m/v62c_tanh_squashed_gaussian_10m_step_007340032.pkl
```

## Evidence

Analysis packet:

```text
experiments/level3_ppo_loop/analysis/2026-06-27_v62d_004_speed_bin_balanced_generator_30m_analysis.md
```

Subagent reviews:

```text
experiments/level3_ppo_loop/analysis/2026-06-27_v62d_004_speed_bin_balanced_generator_30m_subagent_reviews.md
```

Best v62d_004 checkpoint:

```text
5M
lsy_drone_racing/control/checkpoints/v62d_004_speed_bin_balanced_generator_30m/v62d_004_speed_bin_balanced_generator_30m_step_005000000.pkl
```

It is not promoted because:

```text
velocity improvement vs same-distribution v62c 7M is only 5.37%
velocity is worse than the standing v62c 7M default baseline
action_delta worsens materially
all later milestones drift worse than 5M
```

The action path is healthy:

```text
action_clipping=ok
action_sampling_logprob=ok
stored_vs_env_logprob_abs_mean ~= 3.11e-7
```

The remaining blocker is behavior/critic stability:

```text
velocity obedience still weak
late-training drift after 5M
critic values nearly constant late in training
explained variance near zero
```

## Next Candidate

Launch:

```text
v62d_005_speedbin_value_scale10
```

Family:

```text
A_value_return_stabilization
```

Changed knobs:

```text
command_generator_profile=speed_bin_balanced
value_target_scale=10.0
command_vel_error_coef=default
num_minibatches=4
update_epochs=1
```

This intentionally keeps the useful speed-bin generator distribution while
testing a narrower critic target scale than the rejected `value_target_scale=50`
experiments. It does not change observation layout, action distribution, track
config, or reward semantics.

Approved next command is recorded in:

```text
experiments/level3_ppo_loop/v62d_candidates/v62d_005_hypothesis.md
```
