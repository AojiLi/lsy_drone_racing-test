# v62d_008 Decision

Date: 2026-06-27

## Decision

```text
launch_best_of_family_combination
```

Do not promote `v62d_008` as current best, and do not run a 60M confirmation
from this checkpoint.

## Reason

v62d_008 is the first candidate that clearly improves velocity obedience:

```text
v62c 7M velocity error: 0.7397
v62d_008 30M velocity error: 0.5708
relative improvement: 22.84%
```

However, it violates the promotion contract on position error:

```text
v62c 7M position error: 0.6573
v62d_008 30M position error: 0.7943
relative worsening: 20.84%
allowed worsening: <= 5%
```

Cross-track is barely inside the limit:

```text
v62c 7M cross-track: 0.5214
v62d_008 30M cross-track: 0.5449
relative worsening: 4.50%
```

The candidate is valid evidence because:

```text
done_mean improves: 0.00391 -> 0.00219
best-checkpoint action clipping=ok
best-checkpoint action_sampling_logprob=ok
stored_vs_env_logprob_abs_mean=3.26e-7
```

The candidate is not a balanced tracker solution because it trades too much
spatial accuracy for velocity obedience.

## Best Checkpoint

```text
lsy_drone_racing/control/checkpoints/v62d_008_velocity_contrast_constant_speed_generator_30m/v62d_008_velocity_contrast_constant_speed_generator_30m_step_030000000.pkl
```

Best metrics:

```text
reward_mean=-4.6294
command_position_error=0.7943
cross_track_error=0.5449
command_velocity_error=0.5708
done_mean=0.00219
action_delta_penalty=0.0000155
balanced_score=-7.4853
```

## Next Candidate

```text
v62d_009_velocity_contrast_spatial_guarded_generator
```

Family:

```text
E_best_of_family_combination
```

Hypothesis:

```text
Keep v62d_008's paired low/medium/high velocity contrast, but restore
speed-bin-like spatial guards so the tracker does not sacrifice position and
cross-track accuracy to obey speed.
```

Planned changed knob:

```text
command_generator_profile=velocity_contrast_spatial_guarded
```

Generator intent:

```text
keep low/medium/high speed contrast bins
shorten pass_dist / slow_dist / recover_dist versus v62d_008
move decel_fraction closer to speed_bin_balanced behavior, around 0.42
keep longer brake/slow/recover windows than default
keep command references dense and no-gate
```

Keep:

```text
observation_layout=level3_reference_tracker_command_v3
action_distribution=tanh_squashed_gaussian
command_vel_error_coef=default
value_target_scale=1.0
num_envs=1024
num_steps=32
num_minibatches=4
update_epochs=1
train_from_scratch=true
```

Do not add:

```text
gate/aperture/race/finish/stage rewards
gate/obstacle/planner-phase actor inputs
Level3 track config changes
planner action output
```

## Required Gate Before Training

Builder/checker support is required before 30M because this changes generator
semantics.

Checker must verify:

```text
config/level3.toml unchanged
config/level3_tracker_free_space.toml unchanged
new profile exposed through COMMAND_GENERATOR_PROFILES
trainer and audit accept the profile through shared choices
summary/checkpoint metadata records command_generator_profile
observation_layout remains level3_reference_tracker_command_v3
action_distribution remains tanh_squashed_gaussian
reward_coefficients remain {}
no gate/aperture/obstacle/race/finish/stage reward introduced
support smoke finite
action/logprob audit ok
```

Do not launch `v62d_009` 30M until support smoke and checker pass.
