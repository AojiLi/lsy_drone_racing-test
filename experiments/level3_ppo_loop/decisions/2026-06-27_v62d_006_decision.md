# v62d_006 Decision

Date: 2026-06-27

## Decision

Outcome:

```text
launch_best_of_family_combination
```

Do not promote `v62d_006_speedbin_longrollout_256x128_30m`.

Current formal frontier remains:

```text
v62c_7m
lsy_drone_racing/control/checkpoints/v62c_tanh_squashed_gaussian_10m/v62c_tanh_squashed_gaussian_10m_step_007340032.pkl
```

The best speed-bin local signal remains:

```text
v62d_004 5M
lsy_drone_racing/control/checkpoints/v62d_004_speed_bin_balanced_generator_30m/v62d_004_speed_bin_balanced_generator_30m_step_005000000.pkl
```

## Evidence

Analysis packet:

```text
experiments/level3_ppo_loop/analysis/2026-06-27_v62d_006_speedbin_longrollout_256x128_30m_analysis.md
```

Subagent reviews:

```text
experiments/level3_ppo_loop/analysis/2026-06-27_v62d_006_speedbin_longrollout_256x128_30m_subagent_reviews.md
```

Best v62d_006 checkpoint:

```text
20M
lsy_drone_racing/control/checkpoints/v62d_006_speedbin_longrollout_256x128_30m/v62d_006_speedbin_longrollout_256x128_30m_step_020000000.pkl
```

Best checkpoint metrics:

| Metric | v62d_006 20M |
|---|---:|
| reward | -2.0644 |
| command position error | 0.1964 |
| cross-track error | 0.1640 |
| command velocity error | 0.7789 |
| done mean | 0.0000 |
| action delta | 0.000092 |
| balanced score | -3.2878 |

It is not promoted because:

```text
velocity is 5.3% worse than v62c 7M default
velocity is 0.6% worse than v62d_004 5M
balanced score is slightly worse than v62d_004 5M
30M/final drifted down sharply from 20M
the 256x128 rollout is about 3x slower than 1024x32
the run does not meet the 10%-15% velocity promotion threshold
```

The best-checkpoint audit is clean:

```text
action_clipping=ok
action_sampling_logprob=ok
advantage_scale=ok
reward_scale=ok
stored_vs_env_logprob_abs_mean=3.20e-7
```

So the rejection is behavioral, not an action-distribution failure.

## Next Candidate

Launch support/checker for:

```text
v62d_007_speedbin_velocity_coef_2x_30m
```

Family:

```text
E_best_of_family_combination
```

Changed knobs:

```text
command_generator_profile=speed_bin_balanced
command_vel_error_coef=1.2
num_envs=1024
num_steps=32
value_target_scale=1.0
num_minibatches=4
update_epochs=1
```

Rationale:

```text
v62d_004 gave the best generator/spatial signal.
v62d_003 gave the only direct velocity-coefficient signal, though too weak alone.
v62d_006 showed longer rollout does not solve velocity obedience.
```

Because v62d_007 combines generator and reward-number knobs, run a small
support smoke/checker before the 30M training command even though no new code is
required. The checker must verify:

```text
config/level3.toml unchanged
config/level3_tracker_free_space.toml unchanged
observation_layout=level3_reference_tracker_command_v3
action_distribution=tanh_squashed_gaussian
reward_coefficients only override generic command vel_error_coef=1.2
command_generator_profile=speed_bin_balanced
checkpoint metadata records the combined knobs
action/logprob audit remains ok
```

Do not add gate, aperture, race, finish, stage, obstacle, or planner-phase actor
inputs/rewards.
