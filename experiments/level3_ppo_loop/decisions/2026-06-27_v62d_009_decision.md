# v62d_009 Decision

Date: 2026-06-27

## Decision

```text
hold_for_user_review
```

Do not promote `v62d_009` as current best, and do not run a 60M confirmation
from this checkpoint.

## Reason

v62d_009 was designed to combine:

```text
v62d_008 velocity contrast
+ speed-bin-like spatial guards
```

The candidate did restore spatial discipline at the best balanced checkpoint,
but it lost the velocity breakthrough.

Best balanced checkpoint:

```text
15M
lsy_drone_racing/control/checkpoints/v62d_009_velocity_contrast_spatial_guarded_generator_30m/v62d_009_velocity_contrast_spatial_guarded_generator_30m_step_015000000.pkl
```

Compared with the v62c 7M frontier:

| metric | v62c 7M | v62d_009 15M | result |
|---|---:|---:|---|
| reward | -4.8459 | -4.8927 | worse |
| position error | 0.6573 | 0.6570 | tied |
| cross-track | 0.5214 | 0.4833 | better |
| velocity error | 0.7397 | 0.7811 | worse |
| done mean | 0.00391 | 0.00391 | tied |
| balanced score | -7.5365 | -7.5566 | slightly worse |

Promotion threshold fails because velocity should improve by at least
`10%-15%`; v62d_009 `15M` worsens velocity by `5.59%`.

The best velocity checkpoint:

```text
30M / final
```

has:

```text
command_velocity_error = 0.7021
```

That improves velocity by only about `5.08%` versus v62c 7M, still below the
promotion threshold, while position and cross-track collapse:

```text
position error: 0.6573 -> 0.9161
cross-track error: 0.5214 -> 0.7357
```

## Best Checkpoint

The best diagnostic checkpoint inside v62d_009 is:

```text
lsy_drone_racing/control/checkpoints/v62d_009_velocity_contrast_spatial_guarded_generator_30m/v62d_009_velocity_contrast_spatial_guarded_generator_30m_step_015000000.pkl
```

Best metrics:

```text
reward_mean=-4.8927
command_position_error=0.6570
cross_track_error=0.4833
command_velocity_error=0.7811
done_mean=0.00391
action_delta_penalty=0.0000122
balanced_score=-7.5566
```

This checkpoint is diagnostic only. It does not replace v62c 7M as the
frontier.

## Audit

Best-checkpoint action/logprob audit passed for the `15M` checkpoint:

```text
action_clipping=ok
action_sampling_logprob=ok
advantage_scale=ok
initial_std=ok
reward_scale=ok
stored_vs_env_logprob_abs_mean=3.18e-7
sample_clip_fraction=0.0
```

PPO caveat:

```text
final explained variance ~= 5.45e-5
final values std ~= 0.0025
final returns std ~= 41.07
```

The critic remains nearly state-constant. This is now a repeated pattern across
v62d candidates.

## Next Action

Do not launch another immediate 30M generator-only candidate.

Run a v62d meta-review before v62d_010. The meta-review should compare at least:

```text
v62c 7M
v62d_003 velocity reward
v62d_004 speed-bin generator
v62d_008 velocity contrast generator
v62d_009 spatial-guarded contrast generator
```

Required questions:

```text
1. Is the next useful knob critic/value normalization or return scaling?
2. Is a staged generator curriculum needed instead of one mixed distribution?
3. Is the velocity/spatial tradeoff coming from generator distribution,
   reward balance, PPO update pressure, or weak critic learning?
4. What single next v62d_010 knob should be tested after the review?
```

The current frontier remains:

```text
v62c 7M
lsy_drone_racing/control/checkpoints/v62c_tanh_squashed_gaussian_10m/v62c_tanh_squashed_gaussian_10m_step_007340032.pkl
```

The v62d high-budget search remains active; this decision is a hold for
meta-review, not completion of the goal.
