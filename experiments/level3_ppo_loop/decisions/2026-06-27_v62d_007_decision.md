# v62d_007 Decision

Date: 2026-06-27

## Decision

Outcome:

```text
switch_candidate_family
```

Do not promote:

```text
v62d_007_speedbin_velocity_coef_2x_30m
```

Do not use the final checkpoint and do not continue this candidate to 60M.

## Evidence

Analysis packet:

```text
experiments/level3_ppo_loop/analysis/2026-06-27_v62d_007_speedbin_velocity_coef_2x_30m_analysis.md
```

Subagent review packet:

```text
experiments/level3_ppo_loop/analysis/2026-06-27_v62d_007_speedbin_velocity_coef_2x_30m_subagent_reviews.md
```

Generated milestone eval artifacts, intentionally not committed:

```text
experiments/level3_ppo_loop/analysis/tracker_stage_metrics/v62d_007_speedbin_velocity_coef_2x_30m_milestone_eval.json
experiments/level3_ppo_loop/analysis/tracker_stage_metrics/v62d_007_speedbin_velocity_coef_2x_30m_milestone_eval.csv
experiments/level3_ppo_loop/analysis/tracker_stage_metrics/v62d_007_speedbin_velocity_coef_2x_30m_best_audit.json
```

Best checkpoint:

```text
lsy_drone_racing/control/checkpoints/v62d_007_speedbin_velocity_coef_2x_30m/v62d_007_speedbin_velocity_coef_2x_30m_step_015000000.pkl
```

Best metrics:

```text
reward_mean=-2.5724
command_position_error=0.2026
cross_track_error=0.1710
command_velocity_error=0.7943
done_mean=0.0
action_delta_penalty=0.0000875
balanced_score=-3.8301
```

Post-audit:

```text
action_clipping=ok
action_sampling_logprob=ok
advantage_scale=ok
reward_scale=ok
stored_vs_env_logprob_abs_mean=3.23e-7
```

## Why It Is Rejected

The action/logprob path is healthy, so this is not a tanh distribution or
clipping issue.

The candidate fails the promotion threshold:

```text
velocity is 7.4% worse than v62c 7M default
velocity is 2.6% worse than v62d_004 best
velocity is 2.0% worse than v62d_006 best
velocity is 10.0% worse than v62d_003 best
```

The final/30M checkpoint also drifts badly:

```text
15M velocity_error=0.7943
30M/final velocity_error=1.0491
15M action_delta=0.0000875
30M/final action_delta=0.000456
```

Therefore, combining `speed_bin_balanced` with `command_vel_error_coef=1.2`
does not solve velocity obedience.

## Next Candidate

Launch a new Family C generator-distribution candidate after builder/checker
support:

```text
v62d_008_velocity_contrast_constant_speed_generator
```

The next single knob should be:

```text
command_generator_profile=velocity_contrast_constant_speed
```

Keep:

```text
observation_layout=level3_reference_tracker_command_v3
action_distribution=tanh_squashed_gaussian
command_vel_error_coef=default
value_target_scale=1.0
num_envs=1024
num_steps=32
num_minibatches=4
update_epochs=1
train_from_scratch=true
```

Do not add:

```text
gate/aperture/race/finish/stage rewards
gate/obstacle/planner-phase actor inputs
Level3 track config changes
```

Builder/checker is required because the new generator profile changes command
semantics.
