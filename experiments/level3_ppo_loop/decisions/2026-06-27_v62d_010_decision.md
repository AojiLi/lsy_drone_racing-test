# v62d_010 Decision

Date: 2026-06-27

## Decision

```text
hold_for_10_candidate_meta_review
```

Do not promote `v62d_010` as current best, do not continue it to 60M, and do
not launch `v62d_011` until the required 10-candidate v62d meta-review is
written.

## Reason

v62d_010 was designed to combine:

```text
v62d_008 velocity-contrast constant-speed generator
+ generic cross-track reward guard
```

The candidate did not produce a checkpoint that keeps both sides of the tradeoff.

Best balanced checkpoint:

```text
5M
lsy_drone_racing/control/checkpoints/v62d_010_velocity_contrast_cross_track_guard_30m/v62d_010_velocity_contrast_cross_track_guard_30m_step_005000000.pkl
```

Compared with the same-profile v62c 7M eval:

| metric | v62c same-profile | v62d_010 5M | result |
|---|---:|---:|---|
| reward | -5.4641 | -5.8413 | worse |
| position error | 0.7297 | 0.6613 | better |
| cross-track | 0.5185 | 0.4946 | better |
| velocity error | 0.7954 | 0.9339 | worse |
| done mean | 0.00391 | 0.00586 | worse |
| balanced score | -8.3369 | -8.6649 | worse |

The best velocity checkpoint:

```text
30M / final
```

has:

```text
command_velocity_error = 0.6061
```

That is about `18.07%` better than the formal v62c 7M frontier velocity error,
but it fails the spatial guardrails:

```text
position error: 0.6573 -> 0.9581
cross-track error: 0.5214 -> 0.7451
balanced score: -7.5365 -> -9.9886
```

It also does not beat v62d_008's same-profile velocity point:

```text
v62d_008 30M velocity error = 0.5708
v62d_010 30M velocity error = 0.6061
```

## Best Checkpoint

The best diagnostic checkpoint inside v62d_010 is:

```text
lsy_drone_racing/control/checkpoints/v62d_010_velocity_contrast_cross_track_guard_30m/v62d_010_velocity_contrast_cross_track_guard_30m_step_005000000.pkl
```

Best metrics:

```text
reward_mean=-5.8413
command_position_error=0.6613
cross_track_error=0.4946
command_velocity_error=0.9339
done_mean=0.00586
action_delta_penalty=0.0000179
balanced_score=-8.6649
```

This checkpoint is diagnostic only. It does not replace v62c 7M as the
frontier.

## Audit

Best-checkpoint action/logprob audit passed for the `5M` checkpoint:

```text
action_clipping=ok
action_sampling_logprob=ok
advantage_scale=ok
initial_std=ok
reward_scale=ok
stored_vs_env_logprob_abs_mean=3.16e-7
sample_clip_fraction=0.0
```

PPO caveat:

```text
final explained variance ~= 5.81e-5
final values std ~= 0.00431
final returns std ~= 48.62
```

The action path is healthy. The critic remains weak.

## Next Action

Run a 10-candidate v62d meta-review before v62d_011. The review should compare:

```text
v62c 7M
v62d_001
v62d_002
v62d_003
v62d_004
v62d_005
v62d_006
v62d_007
v62d_008
v62d_009
v62d_010
```

Required questions:

```text
1. Which candidate families actually improved one of the core tracker axes?
2. Is the v62d bottleneck mainly velocity/spatial reward conflict, generator
   distribution, weak critic/value learning, or PPO update pressure?
3. Should v62d_011 use a command-conditioned reward/generator fix rather than
   another global coefficient?
4. Should the next candidate be blocked on critic/value learning support first?
```

The current frontier remains:

```text
v62c 7M
lsy_drone_racing/control/checkpoints/v62c_tanh_squashed_gaussian_10m/v62c_tanh_squashed_gaussian_10m_step_007340032.pkl
```

The v62d high-budget search remains active; this decision is a hold for
meta-review, not completion of the goal.
